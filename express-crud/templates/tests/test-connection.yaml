apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "express-crud.fullname" . }}-test-connection"
  labels:
    {{- include "express-crud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "express-crud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
